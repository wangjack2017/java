进程的三种基本状态：
  运行态：占有cpu，并在cpu上运行
  就绪态：已具备运行条件，但cpu没有空闲，暂不能运行
  阻塞态：等待某一个事件的发生
  另外的两个状态：创建态，终止态

进程状态的转化：
    就绪态----运行态：进程被调度
    运行态----就绪态：时间片到，cpu被其他进程占用
    运行态----阻塞态：等待系统资源的分配
    阻塞态----就绪态：资源分配到位，等待cpu调度
    创建态----就绪态:  系统完成创建进程的相关工作
    运行态----终止态：进程运行结束，或运行发生错误
